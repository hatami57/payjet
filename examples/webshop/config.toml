# payjet example — microjet config.
#
# Override any value with env vars (prefix APP), e.g.:
#   APP_HTTP_PORT=9090 APP_DATABASE_HOST=db.internal go run ./example
# Select the gateway and supply credentials under [payjet]; everything
# else (server, database, logging) is plain microjet config.

[app]
namespace = "payjet"
environment = "development"
name = "payjet example shop"
version = "1.0.0"
debug = true

[http]
host = "0.0.0.0"
port = 8080
# Behind a reverse proxy, list its addresses so the access log and per-IP rate
# limiting see the real client IP (X-Forwarded-For is ignored otherwise):
# trustedProxies = ["10.0.0.0/8"]

[database]
host = "localhost"
port = 5432
user = "postgres"
password = "postgres"
name = "payjet"
sslMode = "disable"

[log]
level = "info"
format = "text"

# ── payjet gateway selection ──────────────────────────────────────────────────
# gateway = virtual | zarinpal | idpay | saman | parsian | mellat | pasargad
[payjet]
gateway = "virtual"

[payjet.zarinpal]
merchantID = ""
sandbox = true

[payjet.idpay]
apiKey = ""
sandbox = true

[payjet.saman]
terminalID = ""

[payjet.parsian]
loginAccount = ""

[payjet.mellat]
terminalID = 0
username = ""
password = ""
